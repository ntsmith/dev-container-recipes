This is an alpine container that has access to user git ssh credentials.
It assumes that the environment running the container 
    has ssh-agent running with access to the credentials used to connect to git.
